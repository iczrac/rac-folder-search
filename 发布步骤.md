# 🚀 发布到 GitHub - 简易步骤

## 📋 准备工作

1. **确保有 GitHub 账号**
   - 如果没有，访问 https://github.com/signup 注册

2. **确保已安装 Git**
   ```bash
   git --version
   ```

---

## 🎯 方法 1：使用自动化脚本（推荐）

### 步骤 1：在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 填写信息：
   - Repository name: `rac-folder-search`
   - Description: `Fast folder search with symlink support for VS Code`
   - 选择 **Public**
   - ❌ 不要勾选任何选项（README、.gitignore、License）
3. 点击 "Create repository"

### 步骤 2：运行发布脚本

在终端执行（替换为你的 GitHub 用户名）：

```bash
./发布到GitHub.sh 你的GitHub用户名
```

**示例**：
```bash
./发布到GitHub.sh RaCHEN
```

### 步骤 3：创建 Release

1. 访问你的仓库：`https://github.com/你的用户名/rac-folder-search`
2. 点击 "Releases" → "Create a new release"
3. 填写信息：
   - Tag: `v1.0.2`
   - Title: `RAC Folder Search v1.0.2`
   - Description: 复制 `GitHub发布指南.md` 中的 Release 描述
4. 上传 `rac-folder-search-1.0.2.vsix` 文件
5. 点击 "Publish release"

---

## 🎯 方法 2：手动执行命令

### 步骤 1：在 GitHub 创建仓库（同上）

### 步骤 2：初始化并推送

在终端执行（替换为你的 GitHub 用户名）：

```bash
# 1. 初始化 Git
git init

# 2. 添加远程仓库
git remote add origin https://github.com/你的用户名/rac-folder-search.git

# 3. 添加文件
git add .

# 4. 提交
git commit -m "Initial commit: RAC Folder Search v1.0.2"

# 5. 推送
git branch -M main
git push -u origin main
```

### 步骤 3：创建 Release（同上）

---

## 🔑 认证配置（如果需要）

如果推送时要求输入密码，需要使用 Personal Access Token：

### 创建 Token

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 点击 "Generate token"
5. **复制 token**（只显示一次！）

### 使用 Token

推送时：
- Username: 你的 GitHub 用户名
- Password: 粘贴你的 Personal Access Token

---

## ✅ 验证发布

发布后，检查：

1. **仓库可访问**
   ```
   https://github.com/你的用户名/rac-folder-search
   ```

2. **代码已上传**
   - 查看文件列表
   - 确认所有文件都在

3. **Release 可下载**
   ```
   https://github.com/你的用户名/rac-folder-search/releases
   ```
   - 确认 v1.0.2 存在
   - 确认 .vsix 文件可下载

---

## 📝 更新 package.json

发布后，更新 `package.json` 中的仓库地址：

```json
{
  "repository": {
    "type": "git",
    "url": "https://github.com/你的GitHub用户名/rac-folder-search"
  }
}
```

然后提交更新：

```bash
git add package.json
git commit -m "Update repository URL"
git push
```

---

## 🎉 完成！

现在你的插件已经在 GitHub 上了！

**仓库地址**：`https://github.com/你的用户名/rac-folder-search`

**其他人可以**：
- 查看源代码
- 下载 .vsix 文件
- 提交 Issue
- Fork 和贡献代码

---

## 📚 相关文档

- **详细指南**：`GitHub发布指南.md`
- **自动化脚本**：`发布到GitHub.sh`
- **插件文档**：`README.zh-CN.md`

---

## 💡 提示

1. **首次推送**可能需要配置 Git 用户信息：
   ```bash
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```

2. **如果遇到问题**，查看 `GitHub发布指南.md` 的"常见问题"部分

3. **保护你的 Token**，不要分享给他人

---

**祝发布顺利！🚀**
