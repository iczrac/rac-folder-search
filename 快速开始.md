# 🚀 快速开始 - RAC Folder Search

## 📦 安装（3 步）

1. **打开 VS Code**
2. **按 `Cmd+Shift+X`** 打开扩展面板
3. **点击 `...`** → "Install from VSIX..." → 选择 `rac-folder-search-1.0.2.vsix`

## 🔍 使用（1 步）

**按 `Cmd+Alt+F`**（Mac）或 `Ctrl+Alt+F`（Windows/Linux）→ 输入案件编号（如 `C2CAR2021`）

### 其他方式

1. **按 `Cmd+Shift+P`**
2. **输入 "RAC"** → 选择 "RAC Folder Search: Search Folders"

## ⚙️ 配置（可选）

按 `Cmd+,` 打开设置，搜索 `fold-search`：

```json
{
  "fold-search.followSymlinks": true,    // 跟踪符号链接
  "fold-search.maxDepth": 2,             // 只索引第一层
  "fold-search.includeFiles": false,     // 只显示文件夹
  "fold-search.maxResults": 5000         // 最多 5000 项
}
```

## 📝 需要修改的信息（如果要发布）

打开 `package.json`，修改：

```json
{
  "publisher": "你的发布者名称",  // 当前是 "RaCHEN"
  "repository": {
    "url": "https://github.com/你的用户名/rac-folder-search"
  }
}
```

**详细说明**：查看 `PUBLISHER_GUIDE.md`

## ⌨️ 快捷键

| 功能 | Mac | Windows/Linux |
|------|-----|---------------|
| **搜索文件夹** | `Cmd+Alt+F` | `Ctrl+Alt+F` |

**详细说明**：查看 `快捷键说明.md`

## 📚 更多文档

- `快捷键说明.md` - 快捷键使用指南 ⭐
- `FINAL_SETUP.md` - 完整安装配置指南
- `README.zh-CN.md` - 中文使用说明
- `PUBLISHER_GUIDE.md` - 发布者配置指南

## ✅ 完成！

现在可以按 `Cmd+Alt+F` 快速搜索你的 1717 个案件文件夹了！🎉
