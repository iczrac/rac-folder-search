name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 1.0.3)'
        required: true
        default: '1.0.3'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Compile TypeScript
      run: npm run compile
      
    - name: Run tests
      run: npm test || echo "Tests failed or not configured, continuing..."
      
    - name: Install vsce
      run: npm install -g @vscode/vsce
      
    - name: Package extension
      run: vsce package --allow-missing-repository
      
    - name: Get version from tag
      id: get_version
      run: |
        if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
          echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
        else
          echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        fi
        
    - name: Publish to VS Code Marketplace
      if: secrets.VSCE_PAT
      run: |
        echo "Publishing to VS Code Marketplace..."
        vsce publish --pat ${{ secrets.VSCE_PAT }}
      env:
        VSCE_PAT: ${{ secrets.VSCE_PAT }}
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.get_version.outputs.version }}
        name: RAC Folder Search v${{ steps.get_version.outputs.version }}
        body: |
          ## 🎉 RAC Folder Search v${{ steps.get_version.outputs.version }}
          
          Fast folder and file search with full symbolic link support for VS Code.
          
          ### ✨ Features
          
          - 🔍 **Fast Search**: 6ms scan time for 1700+ folders
          - ⌨️ **Keyboard Shortcut**: `Cmd+Alt+F` (Mac) / `Ctrl+Alt+F` (Windows/Linux)
          - 🔗 **Symlink Support**: Full symbolic link support with circular reference detection
          - 📊 **Smart Sorting**: Exact match, prefix match, and intelligent ranking
          - 🚀 **Caching**: Instant subsequent searches (< 50ms)
          - ⚙️ **Configurable**: Depth limit, exclude patterns, and more
          
          ### 📦 Installation
          
          **Method 1: VS Code Marketplace (Recommended)**
          1. Open VS Code
          2. Press `Cmd+Shift+X` to open Extensions panel
          3. Search for "RAC Folder Search"
          4. Click Install
          
          **Method 2: Manual Installation**
          1. Download `rac-folder-search-${{ steps.get_version.outputs.version }}.vsix` from the assets below
          2. Open VS Code
          3. Press `Cmd+Shift+X` to open Extensions panel
          4. Click `...` → "Install from VSIX..."
          5. Select the downloaded `.vsix` file
          
          ### ⌨️ Usage
          
          Press `Cmd+Alt+F` (Mac) or `Ctrl+Alt+F` (Windows/Linux) to open folder search.
          
          ### 🔧 Configuration
          
          ```json
          {
            "fold-search.followSymlinks": true,
            "fold-search.maxDepth": 2,
            "fold-search.includeFiles": false,
            "fold-search.maxResults": 5000
          }
          ```
          
          ### 📊 Performance
          
          - **Initial scan**: 6ms ⚡
          - **Cached search**: < 50ms ⚡
          - **Memory usage**: < 10MB ⚡
          
          ### 🆕 What's New in v${{ steps.get_version.outputs.version }}
          
          - ✨ Available on VS Code Marketplace
          - ✨ Enhanced keywords for better discoverability
          - ✨ Automatic publishing workflow
          
          ### 📝 Requirements
          
          - VS Code 1.75.0 or higher
          
          ### 📄 License
          
          MIT License
          
          ---
          
          **Marketplace**: https://marketplace.visualstudio.com/items?itemName=iczrac.rac-folder-search
          
          **Full Changelog**: https://github.com/iczrac/rac-folder-search/commits/v${{ steps.get_version.outputs.version }}
        files: |
          rac-folder-search-${{ steps.get_version.outputs.version }}.vsix
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}